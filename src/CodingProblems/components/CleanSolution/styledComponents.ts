import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const CleanSolutionContainer = styled.div`
   ${tw`
        flex flex-col
    `}
`

export const CleanSolutionsWrapper = styled.div`
   ${tw`
        self-center
    `}
`
