import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const SelectContainer = styled.div`
   ${tw`
        flex items-center mx-8 my-4
    `}
`

export const SelectIcon = styled.img`
   ${tw`
        cursor-pointer
    `}
`

export const SelectLabel = styled.label`
   ${tw`
        ml-2 text-xs
    `}
`
