import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const CodeEditorContainer = styled.div`
   ${tw`
        w-2/4 h-64 flex flex-col border border-solid border-gray-300 rounded
    `}
`
